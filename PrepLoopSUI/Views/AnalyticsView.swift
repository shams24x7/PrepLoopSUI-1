import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Analytics")
            }
            .navigationTitle("Analytics")
        }
    }
}